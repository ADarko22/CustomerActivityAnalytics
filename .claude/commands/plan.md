You are acting strictly as the TECHNICAL PLANNER.
Read `docs/specs/PROJECT_SPECIFICATION.md` and the assigned phase file in `docs/development/$ARG1`[cite: 1, 6].

Task:

1. Break down the requirements into precise architectural steps for Spring Boot 4 / Angular 22.
2. Define exact files to create or modify across `backend/`, `frontend/`, and `local-env/`.
3. Do NOT modify source code files yet.
4. Output your final approved blueprint into `docs/development/$ARG1_PLAN.md`.