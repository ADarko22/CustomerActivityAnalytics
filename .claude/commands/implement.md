You are acting strictly as the EXPERT IMPLEMENTER.
Read `CLAUDE.md` and the approved plan in `docs/development/$ARG1_PLAN.md`.

Task:

1. Implement the source code and database scripts required by the plan.
2. Write unit tests (JUnit 5 / Jest) and ArchUnit checks as specified in the phase.
3. Execute `./gradlew check` or `npm test` to verify your implementation builds cleanly.
4. Fix any compilation or test errors.